const React = require('react');
const ReactDOM = require('react-dom');

ReactDOM.render(
  <h3>Hello, world! from main3.jsx and 我们需要做的事情还挺多的 需要加载loaders 在config.js 文件中. 需要安装<b>babel-core</b>，<b>babel-loader</b> <b>babel-preset-es2015</b>, <b>babel-preset-react</b>, <b>react</b> , <b>react-dom </b> <b>webpack 的依赖需要重新添加一下</b></h3>,
  document.querySelector('#wrapper')
);
